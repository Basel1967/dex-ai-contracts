# Contributing

Do not infer semantics. Every field, fixture and test must link to a normative locator. Add unresolved semantics to the
Missing Source Register. Run `make all`. Generated output is derivative, never normative. No production data or secrets.
